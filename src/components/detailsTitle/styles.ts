import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    alignItems: 'center',
    marginVertical: 12,
  },
  title: {
    fontSize: 16,
    fontWeight: 'bold',
  },
});
